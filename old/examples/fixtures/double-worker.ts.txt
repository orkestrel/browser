/**
 * Worker thread fixture: doubles the input context.
 * Demonstrates basic exposeNodeWorker usage with the execution context.
 *
 * Published consumers would import directly:
 *   import { exposeNodeWorker } from 'keepalive'
 */
import { exposeNodeWorker } from '../../src/workers/expose.ts'

exposeNodeWorker<number, number>((context) => context * 2)
